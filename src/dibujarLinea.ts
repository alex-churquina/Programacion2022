let base1: number;
let altura1: number;
let operacion :number;

function calcularAreaTriangulo(base1:number,altura1:number,operacion:number): number {
operacion = prompt("Ingrese numero")
  if(operacion ==== 1){
base1 = prompt("ingrese base")
altura = prompt("ingrese altura")

   return (base1*altura1)/2;
  }  
    return = String = (infinito);
}
